language: ruby
sudo: false

rvm:
  - 2.6
  - 2.7
  - 3.0
  - ruby-head
  - jruby-9.2.14

env:
  - RSPEC_VERSION=master
  - RSPEC_VERSION='~> 3.10'

bundler_args: "--retry=3 --jobs=3"

script: "bundle exec rake ci --trace"

matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: jruby-9.2.14.0
